import BookCard from "@/components/BookCard";


export default function Books() {

    return (
        <>
            {/* {books.map((book) => (
                <BookCard key={book.id} data={book} />
            ))} */}
        </>
    );
};